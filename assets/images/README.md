Drop your logo here as `logo.png` (a square PNG, transparent background recommended).

The login page loads `assets/images/logo.png`. If the file is missing it falls
back to the gradient badge, so the app keeps working until you add it.
